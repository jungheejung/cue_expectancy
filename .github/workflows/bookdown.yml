name: Build and Deploy Bookdown

on:
  push:
    branches:
      - main

jobs:
  build_and_deploy:
    runs-on: ubuntu-latest


    steps:
    - name: Use Node.js 16
      uses: actions/setup-node@v2.4.0
      with:
        node-version: '16.x'
        
    - name: Checkout code
      uses: actions/checkout@v3
      with:
        ref: ${{ github.ref }}

    - name: Setup R
      uses: r-lib/actions/setup-r@v2
      with:
        r-version: '4.2.2'
        
    - name: Install Pandoc 2.0
      run: |
        wget https://github.com/jgm/pandoc/releases/download/2.0/pandoc-2.0-1-amd64.deb
        sudo dpkg -i pandoc-2.0-1-amd64.deb

    - name: Install dependencies
      run: |
        Rscript -e "install.packages('bookdown')"
        
    - name: Install devtools resolve pandoc
      run: |
        Rscript -e "install.packages('devtools')"
        
    - name: Install rmarkdown
      run: |
        Rscript -e "install.packages('rmarkdown')"
      
    - name: Build Bookdown Project
      run: |
        Rscript -e "setwd('scripts/step02_R_bookdown'); bookdown::render_book('index.Rmd', 'bookdown::gitbook')"

    - name: Deploy to gh-pages
      uses: peaceiris/actions-gh-pages@v3
      with:
        github_token: ${{ secrets.ACTION }}
        publish_dir: scripts/step02_R_bookdown/_book
